# ✅ 최종 제출 전 체크리스트 (내가 직접 보완할 것)

> 교수님 실제 채점 양식(요구사항지시서·개발완료보고서 템플릿)과 대조한 결과입니다.
> **문서/SQL 내용은 모두 완료**되었고, 아래는 **나만 할 수 있는 작업(이름 기입·캡처 등)** 입니다.
> 작성 기준일: 2026-06-01

---

## 🔴 1순위 — 꼭 해야 점수 깎이지 않는 것

### [x] (A) 데이터베이스 이름을 "본인 이름(bokyung)"으로 변경  ✅ 완료
교수님 요구사항: **"데이터베이스 생성 시 이름은 본인의 이름으로 생성한다."**
→ DB 이름을 **`bokyung`**으로 변경 완료(`sql/schema.sql`, `sql/optimization.sql` 전체).
MySQL 8.0.45에서 `CREATE DATABASE bokyung` 실행 및 데이터 적재까지 정상 확인.

### [ ] (B) 각 문서 표지(머리말)에 학번·이름·작성일 기입
교수님 양식의 표지에 `문서번호 / 학번 / 작성자 / 작성일자` 칸이 있음. 현재 비어 있으니 채울 것.
- `docs/01_요구사항지시서.md`, `docs/02_스키마정의서.md`, `docs/03_개발완료보고서.md` 상단

---

## 🟡 2순위 — 캡처(사진) 자료 (추가 점수 + 증빙)

MySQL Workbench에서 `schema.sql` → `optimization.sql`을 실행하고 아래 화면을 캡처해
`screenshots/` 폴더에 저장. (파일명은 `screenshots/README.md`와 동일하게)

### 기본 점수 증빙 (필수)
- [ ] `create_database.png` — DB 생성 쿼리 실행 화면
- [ ] `create_table.png` — 테이블 생성 + `SHOW TABLES;` 결과
- [ ] `insert_data.png` — 데이터 INSERT 완료 화면
- [ ] `select_result.png` — `SELECT *` 결과 (성적표 조회 등)

### 추가 점수 증빙 (View/Procedure/Transaction/Index)
- [ ] `view_result.png` — `SELECT * FROM StudentGradeView;`
- [ ] `procedure_result.png` — `CALL GetStudentGrade(1);`
- [ ] `transaction_result.png` — 롤백 전/후 점수(10.00 → 92.00) 보이게
- [ ] `index_result.png` — `SHOW INDEX FROM Student;` 또는 인덱스 생성 화면

### 최적화 실험 증빙 (차별화 — 강력 추천)
- [ ] `explain_before.png` — 인덱스 전 EXPLAIN (type=**ALL**)
- [ ] `explain_after.png` — 인덱스 후 EXPLAIN (type=**ref**)
- [ ] `performance_before.png` — 인덱스 전 `EXPLAIN ANALYZE` (약 2.72ms)
- [ ] `performance_after.png` — 인덱스 후 `EXPLAIN ANALYZE` (약 0.011ms)

> 📌 캡처 팁: 쿼리문 + 결과(또는 Duration 시간)가 한 화면에 같이 보이게 찍을 것.
> 미리 만들어 둔 실측 출력 텍스트는 `screenshots/실행결과_텍스트증거.md` 참고.

---

## 🟢 3순위 — 있으면 더 좋은 것 (선택)

### [ ] (C) 그래픽 ERD 만들기 (Crow's Foot 다이어그램)
현재 ERD는 텍스트(`docs/erd_description.md`)로 되어 있음. 교수님은 ER 다이어그램 표기를 봄.
- **쉬운 방법**: Workbench 상단 메뉴 → **Database ▸ Reverse Engineer** → 본인 DB 선택
  → 자동으로 Crow's Foot ERD 생성됨 → `erd_diagram.png`로 캡처
- 텍스트 ERD에 이미 PK·관계명(belong/study/tutor/manage)·최소참여가 다 적혀 있어 그대로 옮기면 됨.

### [ ] (D) MySQL 버전 확인
- 이 프로젝트는 **MySQL 8.0 이상**에서 동작(CHECK 제약·EXPLAIN ANALYZE 사용).
- Workbench가 8.0이면 문제 없음. (5.7 이하·MariaDB면 일부 기능 미작동 주의)

---

## ✔ 이미 완료된 것 (확인 끝 — 손댈 필요 없음)

교수님 채점표 대비 **내용은 전부 충족**되어 있고, 실제 MySQL 8.0.45에서 **오류 없이 실행 검증**까지 끝냈습니다.

| 채점 항목 | 상태 | 비고 |
| --- | --- | --- |
| 요구사항지시서 (10점) | ✅ | 목적·개체·강/약개체·관계명·속성5개 모두 충족 |
| 개념적 모델링 (1점) | ✅ | PK 명시 + **속성별 중복가능여부 표** 추가 완료 |
| 논리적 모델링 (1점) | ✅ | **FK별 삽입 설명**(belong/study/...) 추가 완료 |
| 함수종속성·정규화 (1점) | ✅ | 6개 릴레이션 FD + 1·2·3NF·BCNF |
| CREATE / INSERT / SELECT (3점) | ✅ | 실행 EXIT=0, 오류 0건 |
| View / Procedure / Transaction / Index (+4점) | ✅ | 전부 동작 확인 |
| 최적화 실험 | ✅ | 5만 건, 인덱스 247배 향상 실측 |
| **예상 총점** | **20 / 20** | 위 1·2순위(이름·캡처) 마무리 시 확정 |

---

## 📋 요약: 이제 남은 할 일 2가지
1. ~~DB 이름을 본인 이름으로 변경~~ → ✅ **`bokyung`으로 완료**
2. **문서 표지에 학번·이름 기입**
3. **Workbench로 실행 후 화면 캡처**해서 `screenshots/`에 저장

DB명 변경은 끝났습니다. 이제 **문서에 학번/이름 적기 + 화면 캡처**만 하면 제출 완료입니다. 🎉
