# 📋 한글파일(교수님 양식) 배치 가이드

> 교수님이 주신 **문서 1·2·3 양식**의 문항 순서 그대로, 각 칸에 붙여넣을 내용을 정리했습니다.
> 각 "▶ 붙여넣기" 블록을 복사해서 한글파일의 해당 위치에 그대로 넣으면 됩니다.
> (출처 표시는 우리 마크다운 파일의 위치이며, 한글파일엔 답변 본문만 넣으세요.)

---
---

# ■ 문서 1. 요구사항지시서

## 1. 문서 목적 및 범위
▶ 붙여넣기:
> 이 문서는 전남대학교(Chonnam University)의 수강신청 및 성적관리 정보를 저장하고 관리하기 위한
> 데이터베이스의 요구사항 지시서이다. 관리자가 이상 현상 없이 학생·학과·교수·강의 정보를 저장하고
> 수강신청과 성적 데이터를 조작할 수 있도록 설계한다.

## 2.1 DB 정의 — 【요소1】 프로젝트로 하고자 하는 것 (2점)
▶ 붙여넣기:
> 본 프로젝트는 대학의 수강신청·성적관리 업무를 데이터베이스로 구현한다. 학생은 매 학기 강의를
> 수강신청하고, 교수는 강의를 담당하며, 학기 종료 시 수강한 강의에 성적이 부여된다. 파일 기반
> 관리에서 발생하는 데이터 중복·갱신 이상·참조 무결성 훼손 문제를 막기 위해, 정보를 개체별로
> 나누어 저장하고 개체 간 관계를 외래키로 연결한 관계형 데이터베이스를 설계하는 것이 목적이다.

## 2.2 요구사항 지시서

### 【요소2】 각 개체의 속성 서술 (2점) + 【요소3】 강한/약한 개체 (2점)
※ 교수님 양식의 E-1, E-2 … 표 형식에 맞춰 아래를 넣으세요.
※ E-1~E-4는 강한 개체, E-5~E-6은 약한 개체입니다.

▶ 붙여넣기 (개체 표):

| no | 설명 |
| --- | --- |
| **E-1** | 【강한 개체】 학생(Student)은 학번(stuid), 이름(sname), 학년(stu_year), 연락처(phone), 이메일(email)을 가진다. |
| **E-2** | 【강한 개체】 학과(Dept)는 학과코드(deptid), 학과명(dname), 단과대학(college), 사무실(office), 전화(tel)를 가진다. |
| **E-3** | 【강한 개체】 강의(Lecture)는 강의번호(lid), 강의명(lname), 학수번호(lnum), 학점(credit), 학기(semester)를 가진다. |
| **E-4** | 【강한 개체】 교수(Professor)는 교번(pid), 이름(pname), 직급(prof_rank), 이메일(email), 연구실(office)을 가진다. |
| **E-5** | 【약한 개체】 수강신청(Enrollment)은 신청ID(enrollid), 신청일(enroll_date), 상태(status), 학기(semester), 신청구분(enroll_type)을 가진다. 학생과 강의가 있어야만 존재하는 약한 개체이다. |
| **E-6** | 【약한 개체】 성적(Grade)은 성적ID(gradeid), 점수(score), 등급(grade_letter), 평가일(evaluation_date), 비고(remark)를 가진다. 수강신청이 있어야만 존재하는 약한 개체이다. |

> ※ 강한 개체 = 혼자 존재 가능(학생·학과·강의·교수). 약한 개체 = 다른 개체가 있어야 존재(수강신청·성적).

### 【요소4】 개체 간 관계 + 관계대응수 + 최소값(최소참여) 서술 (2점)
▶ 붙여넣기 (관계 표):

| no | 설명 (관계명 / 관계대응수 / 최소참여) |
| --- | --- |
| **R-7** | belong(소속): 모든 학생은 반드시 최소 하나의 학과에 소속되며, 학생이 한 명도 없는 학과는 존재할 수 있다. (학생 N : 1 학과) |
| **R-8** | work_dept(근무): 모든 교수는 반드시 최소 하나의 학과에서 근무하며, 교수가 한 명도 없는 학과는 존재할 수 있다. (교수 N : 1 학과) |
| **R-9** | tutor(지도): 한 교수는 여러 학생을 지도하거나 아무도 지도하지 않을 수 있고, 학생도 지도교수가 없을 수 있다. (교수 1 : N 학생) |
| **R-10** | manage(담당): 강의는 반드시 한 명의 교수가 담당하며, 아무 강의도 담당하지 않는 교수가 있을 수 있다. (교수 1 : N 강의) |
| **R-11** | study(수강): 학생은 여러 강의를 수강하거나 하나도 수강하지 않을 수 있고, 강의는 한 명 이상의 학생이 있어야 진행된다. (학생 M : N 강의) |
| **R-12** | evaluate(평가): 하나의 수강신청에는 최대 하나의 성적이 부여된다. 아직 성적이 없는 수강신청도 있을 수 있다. (수강신청 1 : 1 성적) |

> ※ "최소값(최소참여)"이란: 반드시 참여해야 하면 전체 참여(예: 학생은 반드시 학과에 소속),
> 선택이면 부분 참여(예: 지도교수는 없어도 됨)를 뜻합니다. 위 설명에 모두 녹여 두었습니다.
> ※ 이 관계대응수·최소참여는 **문서 2의 ERD에도 동일하게 표기**해야 합니다.

### 【요소5】 개체 수·속성 수 제한 확인 (2점)
▶ 붙여넣기:
> 개체는 총 6개(강한 개체 4 + 약한 개체 2)이며, 각 개체는 논리적 매핑(외래키 삽입) 이전 기준으로
> 고유 속성을 5개씩 가진다. 따라서 "개체 4~6개, 각 개체 속성 3~5개" 조건을 모두 만족한다.

## 3. 참조문서
▶ 붙여넣기:
> [주교재] MySQL로 배우는 데이터베이스 개론과 실습 - 박우창 외 2인
> [교수님 배포자료] 스키마정의서_예제.pdf

---
---

# ■ 문서 2. 스키마정의서  (출처: `docs/02_스키마정의서.md`, `docs/erd_description.md`)

> 이 문서는 ① ERD, ② 논리적 모델(릴레이션), ③ 외래키 매핑, ④ 관계 매핑 순서로 구성합니다.

## (1) ERD — 관계대응수·최소참여 표기
▶ 배치: `docs/erd_description.md`의 ERD 그림을 넣되, **한글파일에서는 Workbench 역공학으로 만든
Crow's Foot ERD 그림(캡처)을 넣는 것을 권장**합니다.
- ERD에 반드시 표기할 것: 각 테이블의 기본키(PK), 관계선(belong/study/tutor/manage/work_dept/evaluate),
  관계대응수(N:1, 1:N, M:N, 1:1), 최소참여(반드시=실선/이중선, 선택=점선/단일선).

## (2) 논리적 모델 (릴레이션 스키마)
▶ 붙여넣기 (밑줄=PK, *=FK):
```
Dept(deptid, dname, college, office, tel)
Professor(pid, pname, prof_rank, email, office, *deptid)
Student(stuid, sname, stu_year, phone, email, *deptid, *advisor_pid)
Lecture(lid, lname, lnum, credit, semester, *pid)
Enrollment(enrollid, *stuid, *lid, enroll_date, status, semester, enroll_type)
Grade(gradeid, *enrollid, score, grade_letter, evaluation_date, remark)
```

## (3) 외래키(FK) 매핑
▶ 붙여넣기: `docs/02_스키마정의서.md` §4 표 전체(FK 7개 + ON DELETE 규칙).
요약: fk_prof_dept, fk_stu_dept, fk_stu_advisor, fk_lec_prof, fk_enr_stu, fk_enr_lec, fk_grade_enr.

## (4) 관계 매핑
▶ 붙여넣기: `docs/02_스키마정의서.md` §5 관계 매핑 표(R1~R6 → 구현 방법).

> ※ 문서 2의 테이블 상세(컬럼·타입·제약)는 `docs/02_스키마정의서.md` §3을 그대로 옮기면 됩니다.

---
---

# ■ 문서 3. 개발완료보고서  (출처: `docs/03_개발완료보고서.md`)

## 1. 개념적 모델링 — 【요소1】 각 개체 PK 명시 + 모든 속성 중복 가능 여부 (1점)
▶ 붙여넣기: `docs/03_개발완료보고서.md` §2.4의 6개 표를 그대로 넣으세요. (아래는 예시 형식)

> **학생(Student) — 기본키: stuid**
> - stuid : 기본키, 중복 불가(학번은 유일)
> - sname : 중복 가능(동명이인 존재)
> - stu_year : 중복 가능(같은 학년 다수)
> - phone : 중복 가능
> - email : UNIQUE, 중복 불가(이메일은 유일)
>
> (Dept, Professor, Lecture, Enrollment, Grade도 동일 형식 — §2.4 표 전체 복사)

## 2. 논리적 모델링 — 【요소2】 FK/대리키 삽입 + 참조관계 서술 (1점)
▶ 붙여넣기: `docs/03_개발완료보고서.md` §3의 "관계별 외래키(FK) 삽입 설명". 예시:

> R-7 belong (Student N:1 Dept): Student(stuid, sname, …, **deptid(FK)**)
> deptid(FK)는 Dept(deptid)를 참조하는 외래키로, N:1 관계라 N쪽(Student)에 추가하여 학생의
> 소속 학과를 표현한다.
> (R-8 work_dept, R-9 tutor, R-10 manage, R-11 study, R-12 evaluate도 동일하게 — §3 전체 복사)

## 3. 종속성 판단 및 정규화 — 【요소3】 각 릴레이션 함수 종속성 명시 (1점)
▶ 붙여넣기: `docs/03_개발완료보고서.md` §4의 6개 릴레이션 함수 종속성. 예시:

> **Student 릴레이션** : Student(stuid, sname, stu_year, phone, email, *deptid, *advisor_pid)
> 함수 종속성: stuid → sname, stu_year, phone, email, deptid, advisor_pid
> (email → stuid 도 성립: email이 UNIQUE이므로 후보키)
> (Dept, Professor, Lecture, Enrollment, Grade도 §4.1~4.6 전체 복사)

▶ 정규화 설명도 함께: `docs/03_개발완료보고서.md` §5 (1NF→2NF→3NF→BCNF 단계별 + 이상현상 사례).

## 4. SQL 코드 구현 및 무결성 — 【요소4】 DB·테이블 생성 작동 (1점), 【요소5】 INSERT 작동 (1점)
▶ 중요: **데이터베이스 이름은 본인 이름(bokyung)으로 생성**되어 있습니다.

▶ 붙여넣기 (4-1 DB 생성):
```sql
-- 데이터베이스 생성 (본인 이름)
CREATE DATABASE bokyung DEFAULT CHARACTER SET utf8mb4;
USE bokyung;
```
▶ 붙여넣기 (4-2 테이블 생성): `sql/schema.sql`의 CREATE TABLE 6개 + 캡처(create_table.png).
▶ 붙여넣기 (4-3 데이터 삽입): `sql/schema.sql`의 INSERT 문 + 캡처(insert_data.png).

> ※ 캡처: Workbench 실행 화면(create_database.png / create_table.png / insert_data.png)을 함께 붙이세요.

## 5. 데이터베이스 검증 — 【요소6】 SELECT * 작동 (1점)
▶ 붙여넣기:
```sql
-- 각 릴레이션 데이터 확인
SELECT * FROM Student;
SELECT * FROM Dept;
SELECT * FROM Professor;
SELECT * FROM Lecture;
SELECT * FROM Enrollment;
SELECT * FROM Grade;
```
+ 실행 결과 캡처(select_result.png)를 붙이세요.

> ※ (추가점수) 뷰·프로시저·트랜잭션·인덱스·최적화 실험은 `sql/optimization.sql`과
> `docs/03_개발완료보고서.md` §7~§8을 보고서 뒤쪽에 추가로 넣으면 가점 대상입니다.

---
---

# ✅ 한 장 요약 — 어느 답이 어디에 들어가나

| 교수님 양식 위치 | 우리 파일에서 가져올 곳 |
| --- | --- |
| 문서1 · 2.1 요소1 | 01 문서 §1 (목적) |
| 문서1 · 요소2·3 (E-1~E-6) | 01 문서 §2.1·§2.2 (개체·속성, 강/약) |
| 문서1 · 요소4 (R-7~R-12) | 01 문서 §3 관계표 + §4 참여제약 |
| 문서1 · 요소5 | 01 문서 §2.3 (속성 수) |
| 문서2 · ERD | erd_description.md (+ Workbench ERD 캡처) |
| 문서2 · 논리모델/FK/관계 | 02 문서 §2·§4·§5 |
| 문서3 · 1 개념(요소1) | 03 문서 §2.4 (PK·중복 가능 여부) |
| 문서3 · 2 논리(요소2) | 03 문서 §3 (FK 삽입 설명) |
| 문서3 · 3 정규화(요소3) | 03 문서 §4 (FD) + §5 (정규화) |
| 문서3 · 4 SQL(요소4·5) | schema.sql (CREATE/INSERT) + 캡처 |
| 문서3 · 5 검증(요소6) | schema.sql SELECT + 캡처 |
